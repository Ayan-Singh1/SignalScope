// setup the skeleton for repo, had to include this to avoid errors in include folder - ayan

#pragma once
#include <string>

struct Tower {
    std::string id;
    double lat;
    double lon;
    // You can add more fields like network type later if you want
};

